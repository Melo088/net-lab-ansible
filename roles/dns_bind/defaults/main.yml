---
# Defaults del rol dns_bind — sobreescribibles desde group_vars o host_vars
bind_packages:
  - bind
  - bind-utils

bind_service:    named
bind_conf_dir:   /etc/named
bind_zones_dir:  /var/named
bind_conf_file:  /etc/named.conf

bind_user:  named
bind_group: named